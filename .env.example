# 复制为 .env 后按需修改
# cp .env.example .env

# 服务监听端口
PORT=3000

# 服务监听地址，容器内请保持 0.0.0.0（宿主机 127.0.0.1 会导致外部无法访问）
HOST=0.0.0.0

# 保护 API 的密钥，长度必须 >= 24 个字符，请使用随机字符串替换 or $(openssl rand -hex 32)
TOKEN_BROKER_API_KEY=please_replace_with_a_random_secret_of_32_chars
